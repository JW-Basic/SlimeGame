#include "include/GrafikObject.h"

GrafikObject::GrafikObject(unsigned int id,
                    image* imageIn,
                    BlockPosition* blockPositionIn,
                    GrafikObject* grafikObjectIn):
                    id(idIn),
                    image(imageIn),
                    blockPosition(blockPositionIn),
                    
                    

GrafikObject::GrafikObject(unsigned int idIn,
                    image* imageIn,
                    BlockPosition* blockPositionIn):
                    id(idIn),
                    image(imageIn),
                    blockPosition(blockPositionIn),
                    gr
